module.exports = {
	config: {
		name: 'start',
		aliases: ['start Giveaway'], 
        description: "",
		category: "Sorteio"
	},
    run: async (client, message, args) => {
        message.channel.send("Comando em construção")
    }
}